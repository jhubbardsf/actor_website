%h1 Listing admin_videos

%table
  %tr
    %th Title
    %th Video
    %th
    %th
    %th

  - @admin_videos.each do |admin_video|
    %tr
      %td= admin_video.title
      %td= link_to image_tag(admin_video.thumbnail(:thumb)), admin_video.youtube_url, :class => 'fancybox-video', :'data-pjax-ignore' => 'true', :title => admin_video.title, :rel => 'youtube_video'
      %td= link_to 'Show', admin_video
      %td= link_to 'Edit', edit_admin_video_path(admin_video)
      %td= link_to 'Destroy', admin_video, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Video', new_admin_video_path
