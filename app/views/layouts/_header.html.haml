%header
  .title-area
    %h1= 'Iris McLean'
    %h2= 'the official site'
  %nav
    .main-links
      = link_to 'Home', home_path
      = link_to 'Biography', bio_path
      = link_to 'Latest News', news_path
      = link_to 'Photos', photos_path
      = link_to 'Videos', videos_path
      = link_to 'Resume', resume_path
      = link_to 'Links', links_path
      = (link_to 'Sign Out', signout_path, 'data-pjax-ignore' => true) if signed_in?

    - if signed_in?
      .main-links.smaller
        = link_to 'Admin Panel', admin_path
